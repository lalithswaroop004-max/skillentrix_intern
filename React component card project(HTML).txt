React component card project-
App.js
/*
import React, { useState } from "react";
import ContactForm from "./components/ContactForm";
import UserList from "./components/UserList";
import Card from "./components/Card";
import "./App.css";

function App() {
  const [users, setUsers] = useState([]);

  const addUser = (user) => {
    setUsers([...users, user]);
  };

  return (
    <div className="App">
      <h1>React Contact Cards Project</h1>

      <ContactForm addUser={addUser} />

      <UserList users={users} />

      <hr />

      <h2>Mini Card Components</h2>

      <Card title="React Basics" />
      <Card title="JavaScript Essentials" />
      <Card title="Frontend Development" />
    </div>
  );
}

export default App;
*/

ContactForm.js
/*
import React, { useState } from "react";

function ContactForm({ addUser }) {
  const [user, setUser] = useState({
    name: "",
    email: "",
    phone: "",
  });

  const handleChange = (e) => {
    setUser({
      ...user,
      [e.target.name]: e.target.value,
    });
  };

  const handleSubmit = (e) => {
    e.preventDefault();

    addUser(user);

    setUser({
      name: "",
      email: "",
      phone: "",
    });
  };

  return (
    <form onSubmit={handleSubmit}>
      <input
        type="text"
        name="name"
        placeholder="Name"
        value={user.name}
        onChange={handleChange}
        required
      />

      <input
        type="email"
        name="email"
        placeholder="Email"
        value={user.email}
        onChange={handleChange}
        required
      />

      <input
        type="text"
        name="phone"
        placeholder="Phone"
        value={user.phone}
        onChange={handleChange}
        required
      />

      <button>Add Contact</button>
    </form>
  );
}

export default ContactForm;
*/

UserList.js
/*
import React from "react";
import ContactCard from "./ContactCard";

function UserList({ users }) {
  return (
    <div>
      <h2>User List</h2>

      {users.map((user, index) => (
        <ContactCard key={index} user={user} />
      ))}
    </div>
  );
}

export default UserList;
*/

ContactCard.js
/*
import React from "react";

function ContactCard({ user }) {
  return (
    <div className="contact-card">
      <h3>{user.name}</h3>

      <p>Email: {user.email}</p>

      <p>Phone: {user.phone}</p>
    </div>
  );
}

export default ContactCard;
*/

Card.js(Mini project)
/*
import React, { useState } from "react";

function Card({ title }) {
  const [liked, setLiked] = useState(false);

  return (
    <div className="card">
      <h3>{title}</h3>

      <p>{liked ? "Liked ❤️" : "Not Liked 🤍"}</p>

      <button onClick={() => setLiked(!liked)}>
        {liked ? "Unlike" : "Like"}
      </button>
    </div>
  );
}

export default Card;
*/

App.css
/*
.App {
  text-align: center;
  padding: 20px;
  font-family: Arial;
}

form {
  margin-bottom: 20px;
}

input {
  display: block;
  width: 250px;
  margin: 10px auto;
  padding: 8px;
}

button {
  padding: 8px 15px;
  cursor: pointer;
}

.contact-card,
.card {
  width: 300px;
  margin: 15px auto;
  padding: 15px;
  border: 1px solid #333;
  border-radius: 8px;
  background: #f5f5f5;
}
*/

index.js
/*
import React from "react";
import ReactDOM from "react-dom/client";
import App from "./App";
import "./index.css";

const root = ReactDOM.createRoot(document.getElementById("root"));

root.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>
);
*/